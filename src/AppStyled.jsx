import BaseButton from '../StyleedComponents/BaseButton';
import Card from '../StyleedComponents/Card';
import StyledButton from '../StyleedComponents/StyledButton';

export default function App() {
  return (
    <>
      <Card />
      <BaseButton>BaseBtn</BaseButton>
      <StyledButton>StyledBtn</StyledButton>
    </>
  );
}
